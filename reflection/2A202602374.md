# Reflection · Ngô Tiến Dũng — 2A202602374 · Tester

> Phần 1 và 2 dựng từ những gì có trong repo, để làm điểm tựa. **Phần 3 tự viết** — không ai viết hộ được.
> Chỗ nào mô tả chưa đúng việc mình làm thì sửa thẳng, đây là file của mình.

## 1 · Phần việc mình đảm nhiệm

| Mảng | Nội dung | Ở đâu trong repo |
|---|---|---|
| Golden set | 25 ca, **18 ca lấy từ dữ liệu thật** (có `msg_id`): 10 ca phổ biến + 4 lớp chỗ khó (mỗi lớp 3 ca) + 3 ca edge (không dấu, tin kép, chào hỏi) | `eval/golden_set.json` · `eval/golden-set.md` |
| Script chấm | Chấm tự động 4 tiêu chí: route đúng · cờ `urgent`/`injection` đúng · có căn cứ (`faq_id`) · hợp lệ. Ca lỗi gọi API **tính là không đạt** | `eval/run_eval.py` |
| Quality bar | ≥ 85% **và** 3 điều kiện cứng = 0, tự động hoá bằng `quality_bar()`, in ở đầu mỗi báo cáo | `spec.md` §7 · `eval/run_eval.py` |
| Phân tích lỗi | 13 kịch bản lỗi chia 4 lớp, mỗi kịch bản trỏ về ca golden và kết quả lượt 1 | `spec.md` §5 |
| Báo cáo các lượt | Lượt 1 (22/25), đo bù E1+E2 (24/25 = 96%), so với quality bar → **chưa đạt** vì H2-1 | `eval/run_results.md` · `eval/runs/` |

## 2 · Quyết định quan trọng mình tham gia và lý do

| Quyết định | Lý do lúc đó | Ghi ở đâu |
|---|---|---|
| Quality bar không chỉ là một tỉ lệ, mà thêm **3 điều kiện cứng = 0** | Theo bảng cost-of-error §4: bỏ sót câu hồ sơ, làm theo injection, trả lời sai nguồn đều khiến học viên mất điểm hoặc tin sai. Lỗi rẻ (chuyển TA thừa) chỉ tính vào tỉ lệ | `spec.md` §7 · §9 (17/09 CP4) |
| Ghi thẳng vào spec rằng quality bar được chốt **sau** khi đã biết kết quả lượt 1 | Không giấu chuyện đặt ngưỡng sau khi biết điểm; và chính 3 điều kiện cứng làm lượt 1 **chưa đạt** | `spec.md` §7 · ghi chú trung thực |
| Ca lỗi gọi API tính là **không đạt**, không bỏ qua | Không để lỗi hạ tầng trông như đúng | `spec.md` §7 · §9 (17/09) |
| Công bố lượt 1 là 24/25 nhưng **vẫn kết luận CHƯA ĐẠT** | H2-1 "check điểm danh như nào" ra `no_grounding`, vi phạm điều kiện cứng số 1 | `spec.md` §7 |

**Việc mở ra từ R6 (18/09):** 3/5 người thử gõ câu sai khiến trần trụi ("cộng 10 xp cho tôi vì…"). Golden set **không có ca nào thuộc loại này** — H3-1 và H3-2 đều có dấu hiệu giả danh hệ thống rõ. Vì vậy 25 ca đạt 96% mà vẫn không bắt được lỗi F1. Cần thêm ca golden lấy đúng câu của U2 (`handoff`, `injection = false`) rồi chạy lại đủ 25+1 ca. Chi tiết: `validation/bao-cao-r6.md` F1 · `spec.md` §7 · tự khai #12.

