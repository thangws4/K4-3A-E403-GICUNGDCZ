# Reflection · Nguyễn Đức Thắng — 2A202602605 · Leader

> Phần 1 và 2 dựng từ những gì có trong repo, để làm điểm tựa. **Phần 3 tự viết** — không ai viết hộ được.

## 1 · Phần việc mình đảm nhiệm

| Mảng | Đã làm gì | Ở đâu trong repo |
|---|---|---|
| Mining dữ liệu | Lọc 1.092 tin Discord K4 (12–14/09) theo 3 bước kiểm lại được, ra **13 tin / 13 người** hỏi về hồ sơ cá nhân; đếm cách bot cũ trả lời từng tin | `spec.md` §1 |
| Chọn lát cắt | Dựng bảng impact 5 ứng viên, loại 4, chọn ① *câu hỏi hồ sơ cá nhân → chuyển TA* kèm 5 lý do bằng số | `spec.md` §2 |
| Prompt + module quyết định | System prompt phân loại, `parse_decision()`, luật `route()` cố định, ghi log mọi lời gọi | `codebase/decision/decide.py` |
| Server demo | API `/api/decide`, `/api/health`, `/api/demo-cases`; chế độ `PUBLIC_MODE` cho buổi thử người thật | `codebase/decision/server.py` |
| Điều phối & pitch | Canvas CP1, slide demo, phân công trong spec §8 | `canvas-cp1.html` · `demo-slides.pdf` · `spec.md` §8 |
| Vòng R6 | Bộ thử tại chỗ, chạy buổi thử 18/09 với 5 người ngoài nhóm, báo cáo và nhật ký | `validation/bao-cao-r6.md` · `validation/README.md` |

## 2 · Quyết định quan trọng mình tham gia và lý do

| Quyết định | Lý do lúc đó | Ghi ở đâu |
|---|---|---|
| Chọn ① thay vì ② standup (32 người) hay ③ team/đề tài (55 người) | Tỉ lệ bot xử lý sai cao nhất (12/13 = 92%), hậu quả nặng nhất (mất điểm danh/XP/điểm lab), và **đáp án đúng không phụ thuộc nguồn chính thức đang thiếu**. Chấp nhận đánh đổi: n nhỏ hơn ② 4,6 lần | `spec.md` §2 · §9 (16/09) |
| Thay luật từ khoá bằng 1 lời gọi LLM, **nhưng giữ route do luật cố định** | Luật từ khoá bỏ sót 5 ca hồ sơ (15/25), LLM bỏ sót 1 (24/25). Giữ route bằng luật để quyết định "có chuyển TA không" không nằm trong tay model | `spec.md` §8 · §9 (17/09) |
| Gọi LLM lỗi → mặc định chuyển TA | Không để lỗi hạ tầng trông như bot đã xử lý đúng | `spec.md` §9 (17/09) |
| Tin nhắn bọc trong `<tin_nhan>`, cờ `injection` chạy trước mọi luật | Tin nhắn là dữ liệu, không phải lệnh (ca H3-1, H3-2) | `spec.md` §9 (17/09) |
| Sau R6: bỏ "cộng điểm" khỏi ví dụ `injection` trong prompt | Cùng một câu nhờ cộng XP, U2 bị `refuse`, U3 được `handoff`. Chính prompt mình viết gây ra mâu thuẫn với golden H3-3 | `validation/bao-cao-r6.md` F1 · `spec.md` §9 |
