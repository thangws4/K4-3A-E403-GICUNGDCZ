# Reflection · Nguyễn Hải Long — 2A202602471 · Dev

> Phần 1 và 2 dựng từ những gì có trong repo, để làm điểm tựa. **Phần 3 tự viết** — không ai viết hộ được.
> Chỗ nào mô tả chưa đúng việc mình làm thì sửa thẳng, đây là file của mình.

## 1 · Phần việc mình đảm nhiệm

| Mảng | Nội dung | Ở đâu trong repo |
|---|---|---|
| Module quyết định | 1 lời gọi LLM trả JSON `{intent, confidence, record_type, urgent, injection, faq_id, summary, reasons}`; parse, kiểm tra hợp lệ, thử lại khi lỗi tạm thời | `codebase/decision/decide.py` |
| Luật định tuyến | `route()` — các nhánh `handoff`, `clarify`, `answer`, `no_grounding`, `refuse`, `privacy`, `decline`, `chitchat`; ngưỡng 0,75 / 0,45; **luật cố định chứ không để model quyết** | `codebase/decision/decide.py` |
| Server | `/api/decide`, `/api/health`, `/api/faq`, `/api/golden`, `/api/runs`, `/api/eval-case`; chế độ `PUBLIC_MODE` giới hạn lời gọi cho buổi thử người thật | `codebase/decision/server.py` |
| Giao diện | Mock Discord, trang demo 4 bước pipeline có so bot cũ / bot mới, trang eval xem lại từng lượt chạy | `codebase/prototype/` |
| Ghi vết | Mỗi lời gọi ghi prompt, phản hồi thô, kết quả parse và `log_id` vào JSONL | `codebase/logs/` (không commit) · `eval/runs/` (có commit) |

## 2 · Quyết định quan trọng mình tham gia và lý do

| Quyết định | Lý do lúc đó | Ghi ở đâu |
|---|---|---|
| LLM chỉ **phân loại**, luật cố định mới **định tuyến** | Quyết định "có chuyển TA không" là chỗ đắt nhất nếu sai, nên không giao cho model | `spec.md` §4 · §9 (17/09) |
| Thêm `faq_id` vào JSON và chấm cả `faq_id` | Chỉ chấm route thì không bắt được bẫy M75012 (trả luật daily cho câu hỏi lab) | `spec.md` §9 (17/09) |
| Gọi LLM lỗi → trả câu `handoff` mặc định | Chuyển TA là nước đi an toàn khi hệ thống hỏng | `spec.md` §9 (17/09) |
| Chế độ `?tester=U…` gắn mã người thử vào log | Để buổi R6 có câu nguyên văn và route theo từng người, người quan sát khỏi phải chép tay | `codebase/prototype/demo.html` · `validation/export_tester_logs.py` |

**Việc mở ra từ R6 (18/09), chưa làm:** lượt lỗi 429 hiện ra y hệt lượt thành công nên mất dữ liệu của U5 — cần nhãn lỗi kỹ thuật riêng cho chế độ `?tester=`. `retries = 4` khiến một lượt hỏng tốn tới 5 request và bắt người dùng chờ ~82 giây. Chi tiết: `validation/bao-cao-r6.md` F2, F3.

